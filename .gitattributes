# Normalize line endings to LF in the repo so the project behaves identically
# on Windows and inside the Linux Docker containers.
* text=auto eol=lf

# Windows-only scripts keep CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf
